# Manga Service

Современный веб-сервис для чтения и каталогизации манги, построенный с использованием принципов Clean Architecture и Domain-Driven Design (DDD).

## 🛠 Стек технологий

### Backend
- **Python 3.13+**
- **FastAPI**: Современный, быстрый веб-фреймворк для создания API.
- **SQLAlchemy**: ORM для работы с базой данных.
- **Redis**: Используется для кэширования и брокера сообщений.
- **MinIO (S3)**: S3-совместимое объектное хранилище для обложек и страниц манги.
- **FastStream**: Фреймворк для работы с очередями сообщений.

### Frontend
- **Next.js**: (В разработке) React-фреймворк для создания быстрого и интерактивного пользовательского интерфейса.

### Инфраструктура
- **Docker & Docker Compose**: Контейнеризация всех компонентов системы для удобного развертывания и разработки.

## 📂 Структура проекта

Проект разделен на логические модули в соответствии с чистой архитектурой:

```
manga/
├── backend/            # Серверная часть приложения
│   ├── app/
│   │   ├── domain/         # Доменные сущности и правила (Core)
│   │   ├── application/    # Бизнес-логика и Use Cases
│   │   ├── infrastructure/ # Работа с БД, файлами и внешними сервисами
│   │   └── presentation/   # API эндпоинты (FastAPI роутеры)
│   └── ...
├── frontend/           # Клиентская часть (Next.js)
└── docker-compose.yaml # Описание мульти-контейнерной архитектуры
```

## 🚀 Запуск проекта

Для запуска локального окружения вам понадобятся установленные **Docker** и **Docker Compose**.

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/Flavien123/manga.git
   cd manga
   ```

2. **Настройка окружения:**
   Убедитесь, что необходимые переменные окружения заданы (создайте `.env` файл при необходимости).

3. **Запуск сервисов:**
   ```bash
   docker-compose up -d --build
   ```

   После запуска API будет доступно по адресу: `http://localhost:8000/docs` (Swagger UI).

## 🏗 Архитектура

Бэкенд спроектирован с учетом масштабируемости и поддерживаемости:
- **Domain Layer**: Содержит бизнес-сущности и интерфейсы репозиториев. Не имеет внешних зависимостей.
- **Application Layer**: Реализует сценарии использования (Use Cases), оркестрируя работу доменных объектов.
- **Infrastructure Layer**: Реализует интерфейсы, определенные в доменном слое (работу с БД, S3, Redis).
- **Presentation Layer**: Отвечает за обработку HTTP-запросов и взаимодействие с клиентом.

## 📝 Разработка

Для локальной разработки бэкенда:

1. Перейдите в директорию `backend`:
   ```bash
   cd backend
   ```
2. Установите зависимости (рекомендуется использовать виртуальное окружение):
   ```bash
   pip install -e .
   ```
3. Запустите сервер разработки:
   ```bash
   fastapi dev app/main.py
   ```
